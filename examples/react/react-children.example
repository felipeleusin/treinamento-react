import { render } from 'react-dom'

function Painel({ titulo, children }) {
  return <div>
    <h1>{titulo}</h1>
    {children}
  </div>
}

render(<Painel titulo="React!">Conteúdo!</Painel>, document.getElementById('root))
